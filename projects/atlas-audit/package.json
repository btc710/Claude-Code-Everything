{
  "name": "atlas-audit",
  "version": "0.1.0",
  "private": true,
  "description": "Append-only audit log + compliance kit for the Atlas stack. SOC2-style controls — immutable events with monthly segments, residency tagging, redaction tombstones for GDPR/CCPA, soft-delete archive, and a per-resource history export API. JSON-backed sketch; production swaps the segment writer for object storage with WORM/object-lock without touching call sites.",
  "main": "src/index.js",
  "bin": {
    "atlas-audit": "src/cli.js"
  },
  "scripts": {
    "test": "node tests/run-all.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
