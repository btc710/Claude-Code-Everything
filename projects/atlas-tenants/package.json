{
  "name": "atlas-tenants",
  "version": "0.1.0",
  "private": true,
  "description": "Tenant + RBAC service for the Atlas stack. Multi-tenant identity, role-based authorization, hashed bearer tokens, and an append-only audit trail. JSON-backed sketch; production swaps the store layer for Postgres.",
  "main": "src/index.js",
  "bin": {
    "atlas-tenants": "src/cli.js"
  },
  "scripts": {
    "test": "node tests/run-all.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
