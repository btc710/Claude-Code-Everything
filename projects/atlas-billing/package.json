{
  "name": "atlas-billing",
  "version": "0.1.0",
  "private": true,
  "description": "Billing + usage rollup for the Atlas stack. Consumes ledger entries exported from atlas-model-gateway, applies per-tenant pricing, and produces period/slot/project rollups and structured invoices. JSON-backed sketch; production swaps the store layer for Postgres.",
  "main": "src/index.js",
  "bin": {
    "atlas-billing": "src/cli.js"
  },
  "scripts": {
    "test": "node tests/run-all.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
